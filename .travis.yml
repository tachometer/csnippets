language: c
compiler:
  - gcc
script: |
  ./autogen.sh
  ./configure
  ./build.sh

notifications:
  irc:
    channels:
      - "irc.freenode.net#csnippets"
      - "irc.quakenet.org#cplusplus"
    skip_join: true
